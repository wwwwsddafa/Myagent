从零构建一个完整的 智能研究助手（Smart Research Assistant）。这个助手能搜索信息、分析文档、生成报告，并支持多轮对话

需求描述:

用户输入问题
    ↓
[问题分类] → 是简单问题？→ 直接回答
    ↓ 否
[研究流程]
    ├── 搜索相关信息（工具调用）
    ├── 检索本地知识库（RAG）
    ├── 综合分析（LLM 推理）
    └── 生成结构化报告（输出解析）
    ↓
[质量审查] → 不通过？→ 补充研究（循环）
    ↓ 通过
[输出结果] → 支持追问（多轮对话 + 记忆）

涉及到的知识:
 - 文档加载，向量检索(RAG)
 - 对话记忆管理
 - 自定义工具(搜索，文件读写)
 - ReAct Agent范式
 - LangGraph工作流，持久化
 - 多Agent协作


 图的执行流程:
 START
  ↓
[classify] ── simple ──→ [simple_answer] ──→ END
  ↓ research/report
[tool_calling]
  ↓ 有工具调用？
  ├── Yes → [tool_executor] → [collect]
  └── No  → [collect]
                ↓
           [researcher]
                ↓
            [analyst]
                ↓
            [writer]
                ↓
            [review] ── 未通过 ──→ [researcher]（循环）
                ↓ 通过
               END


涉及到的知识点:
┌─────────────────────────────────────────────────┐
│                  用户输入                         │
└────────────────────┬────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│  LangGraph 工作流                                │
│                                                   │
│  ┌──────────┐    ┌──────────┐                     │
│  │ classify │───→│ 简单回答  │ ← LCEL 管道         │
│  │ (路由)   │    └──────────┘                     │
│  └────┬─────┘                                     │
│       ↓                                           │
│  ┌──────────┐    ┌──────────┐                    │
│  │ 搜索工具  │←──→│ 工具执行  │ ← Tools            │
│  └────┬─────┘    └──────────┘                     │
│       ↓                                           │
│  ┌──────────┐                                     │
│  │ 研究节点  │ ← Prompt 模板                       │
│  └────┬─────┘                                     │
│       ↓  研究笔记                                         │
│  ┌──────────┐                                     │
│  │ 分析节点  │ ← LLM 调用                          │
│  └────┬─────┘                                     │
│       ↓ 分析报告                                          │
│  ┌──────────┐                                     │
│  │ 写作节点  │ ← 输出解析                     │
│  └────┬─────┘                                     │
│       ↓                                           │
│  ┌──────────┐                                     │
│  │ 质量审查  │──→ 未通过? → 回到研究（循环）        │
│  └────┬─────┘                                     │
│       ↓ 通过                                      │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│  输出结果 + 记忆持久化                           │
└─────────────────────────────────────────────────┘


技术小结:
LangGraph：定义复杂的工作流图，支持条件路由、循环、持久化
自定义工具：搜索、知识库检索、文件保存, 利用@tool装饰器完成langgraph的工具的注册
RAG：本地知识库的向量检索
LCEL 管道：prompt | llm | parser 链式调用
Prompt 工程：针对不同角色的系统提示
输出解析：Pydantic 结构化输出
状态管理：自定义 State 在节点间传递数据
质量审查循环：确保输出质量

以装饰器形式完成日志开发, 以记录节点和工具执行时间和结果，方便调试和分析。
以回调函数形式完成对LLM调用的监控和记录，包括请求参数、响应内容、耗时等。


名词解释：
RAG: 检索增强生成，用于从本地知识库中检索相关文档，然后根据检索结果生成结构化报告。
     由三步组成 Retrieve->Analyze->Generate
     Retrieve: 从知识库中检索相关文档
     Analyze: 对检索到的文档连同原始问题一起打包给AI的提示词中，并进行分析，提取关键信息
     Generate: 根据分析结果生成结构化报告. 注意：此时AI的推理是基于你给的材料，而不是它凭空编造。

     RAG解决的问题: 1. 幻觉   2. 知识过时   3. 安全性













https://github.com/wlddhj/learn_langchain/blob/main/docs/16-practice-project.md